-- [[ CUSTOM MODERN SCRIPT HUB ENGINE V3 ]] --
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local HttpService = game:GetService("HttpService")
local CoreGui = game:GetService("CoreGui")
local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Lighting = game:GetService("Lighting")

local player = Players.LocalPlayer
local playerScripts = player.PlayerScripts
local controllers = playerScripts:WaitForChild("Controllers", 10)

-- Game Modules
local EnumLibrary = require(ReplicatedStorage.Modules:WaitForChild("EnumLibrary", 10))
if EnumLibrary then EnumLibrary:WaitForEnumBuilder() end

local CosmeticLibrary = require(ReplicatedStorage.Modules:WaitForChild("CosmeticLibrary", 10))
local ItemLibrary = require(ReplicatedStorage.Modules:WaitForChild("ItemLibrary", 10))
local DataController = require(controllers:WaitForChild("PlayerDataController", 10))

-- Global System States
local Toggles = {
    SkinChanger = true,
    CharmChanger = true,
    WrapChanger = true,
    DanceChanger = true,
    ToggleKey = Enum.KeyCode.RightShift
}

local equipped, favorites = {}, {}
local constructingWeapon, viewingProfile = nil, nil
local saveFile = "unlockall/custom_config.json"

-- ==================== BACKEND OPTIMIZED UNLOCK ENGINE ====================

local function cloneCosmetic(name, cosmeticType, options)
    local base = CosmeticLibrary.Cosmetics[name]
    if not base then return nil end
    local data = {}
    for key, value in pairs(base) do data[key] = value end
    data.Name = name
    data.Type = data.Type or cosmeticType
    data.Seed = data.Seed or math.random(1, 1000000)
    
    if EnumLibrary then
        local success, enumId = pcall(EnumLibrary.ToEnum, EnumLibrary, name)
        if success and enumId then data.Enum, data.ObjectID = enumId, data.ObjectID or enumId end
    end
    if options then
        if options.inverted ~= nil then data.Inverted = options.inverted end
        if options.favoritesOnly ~= nil then data.OnlyUseFavorites = options.favoritesOnly end
    end
    return data
end

local function saveConfig()
    if not writefile then return end
    pcall(function()
        local config = {equipped = {}, favorites = favorites}
        for weapon, cosmetics in pairs(equipped) do
            config.equipped[weapon] = {}
            for cosmeticType, cosmeticData in pairs(cosmetics) do
                if cosmeticData and cosmeticData.Name then
                    config.equipped[weapon][cosmeticType] = {
                        name = cosmeticData.Name, seed = cosmeticData.Seed, inverted = cosmeticData.Inverted
                    }
                end
            end
        end
        makefolder("unlockall")
        writefile(saveFile, HttpService:JSONEncode(config))
    end)
end

local function loadConfig()
    if not readfile or not isfile or not isfile(saveFile) then return end
    pcall(function()
        local config = HttpService:JSONDecode(readfile(saveFile))
        if config.equipped then
            for weapon, cosmetics in pairs(config.equipped) do
                equipped[weapon] = {}
                for cosmeticType, cosmeticData in pairs(cosmetics) do
                    local cloned = cloneCosmetic(cosmeticData.name, cosmeticType, {inverted = cosmeticData.inverted})
                    if cloned then cloned.Seed = cosmeticData.seed equipped[weapon][cosmeticType] = cloned end
                end
            end
        end
        favorites = config.favorites or {}
    end)
end

local function isAllowedCosmetic(name, cosmeticType)
    if name:find("MISSING_") then return false end
    local typeLower = cosmeticType and cosmeticType:lower() or ""
    local nameLower = name:lower()
    
    if typeLower == "skin" and Toggles.SkinChanger then return true end
    if (typeLower == "charm" or nameLower:find("charm")) and Toggles.CharmChanger then return true end
    if (typeLower == "wrap" or typeLower == "wrapping" or nameLower:find("wrap")) and Toggles.WrapChanger then return true end
    
    -- Added "Taunt" and "Animation" fallbacks for emotes
    if (typeLower == "dance" or typeLower == "emote" or typeLower == "taunt" or nameLower:find("dance") or nameLower:find("emote") or nameLower:find("taunt")) and Toggles.DanceChanger then return true end
    
    return false
end

-- Hook Engine Injectors
local originalOwnsCosmetic = CosmeticLibrary.OwnsCosmetic
CosmeticLibrary.OwnsCosmetic = function(self, inventory, name, weapon)
    if name:find("MISSING_") then return originalOwnsCosmetic(self, inventory, name, weapon) end
    local cosmetic = CosmeticLibrary.Cosmetics[name]
    if cosmetic and isAllowedCosmetic(name, cosmetic.Type) then return true end
    return originalOwnsCosmetic(self, inventory, name, weapon)
end

local originalGet = DataController.Get
DataController.Get = function(self, key)
    local data = originalGet(self, key)
    
    -- THE EMOTE FIX: Forcefully inject items into the table so `pairs()` loops can see them
    if key == "CosmeticInventory" then
        local proxy = type(data) == "table" and table.clone(data) or {}
        
        for k, cosmetic in pairs(CosmeticLibrary.Cosmetics) do
            if isAllowedCosmetic(k, cosmetic.Type) then
                proxy[k] = true -- Actually inserts it so UI loops render it
            end
        end
        
        return setmetatable(proxy, {__index = function(t, k)
            local cosmetic = CosmeticLibrary.Cosmetics[k]
            if cosmetic and isAllowedCosmetic(k, cosmetic.Type) then return true end
            return type(data) == "table" and data[k] or nil
        end})
    end
    
    if key == "FavoritedCosmetics" then
        local result = data and table.clone(data) or {}
        for weapon, favs in pairs(favorites) do
            result[weapon] = result[weapon] or {}
            for name, isFav in pairs(favs) do 
                local cosmetic = CosmeticLibrary.Cosmetics[name]
                if cosmetic and isAllowedCosmetic(name, cosmetic.Type) then result[weapon][name] = isFav end
            end
        end
        return result
    end
    return data
end

local originalGetWeaponData = DataController.GetWeaponData
DataController.GetWeaponData = function(self, weaponName)
    local data = originalGetWeaponData(self, weaponName)
    if not data then return data end
    local merged = {}
    for key, value in pairs(data) do merged[key] = value end
    merged.Name = weaponName
    if equipped[weaponName] then
        for cosmeticType, cosmeticData in pairs(equipped[weaponName]) do 
            if cosmeticType == "Skin" or cosmeticType == "Charm" or cosmeticType == "Wrap" or cosmeticType == "Wrapping" then 
                merged[cosmeticType] = cosmeticData 
            end
        end
    end
    return merged
end

-- Client Interaction Layer (Wraps & Charms fix)
local ClientItem
pcall(function() ClientItem = require(player.PlayerScripts.Modules.ClientReplicatedClasses.ClientFighter.ClientItem) end)
if ClientItem and ClientItem._CreateViewModel then
    local originalCreateViewModel = ClientItem._CreateViewModel
    ClientItem._CreateViewModel = function(self, viewmodelRef)
        local weaponName = self.Name
        local weaponPlayer = self.ClientFighter and self.ClientFighter.Player
        constructingWeapon = (weaponPlayer == player) and weaponName or nil
        
        if weaponPlayer == player and equipped[weaponName] and viewmodelRef then
            local dataKey = self:ToEnum("Data")
            local nameKey = self:ToEnum("Name")
            
            local targetData = viewmodelRef[dataKey] or viewmodelRef.Data
            if targetData then
                if equipped[weaponName].Skin and Toggles.SkinChanger then
                    local skinKey = self:ToEnum("Skin")
                    if viewmodelRef[dataKey] then 
                        viewmodelRef[dataKey][skinKey] = equipped[weaponName].Skin 
                        viewmodelRef[dataKey][nameKey] = equipped[weaponName].Skin.Name
                    else 
                        viewmodelRef.Data.Skin = equipped[weaponName].Skin 
                        viewmodelRef.Data.Name = equipped[weaponName].Skin.Name 
                    end
                end
                if equipped[weaponName].Wrap and Toggles.WrapChanger then
                    local wrapKey = self:ToEnum("Wrap")
                    if viewmodelRef[dataKey] then viewmodelRef[dataKey][wrapKey] = equipped[weaponName].Wrap 
                    else viewmodelRef.Data.Wrap = equipped[weaponName].Wrap end
                end
                if equipped[weaponName].Charm and Toggles.CharmChanger then
                    local charmKey = self:ToEnum("Charm")
                    if viewmodelRef[dataKey] then viewmodelRef[dataKey][charmKey] = equipped[weaponName].Charm 
                    else viewmodelRef.Data.Charm = equipped[weaponName].Charm end
                end
            end
        end
        local result = originalCreateViewModel(self, viewmodelRef)
        constructingWeapon = nil
        return result
    end
end

-- Metamethod Packets Interceptor
if hookmetamethod then
    local remotes = ReplicatedStorage:FindFirstChild("Remotes")
    local dataRemotes = remotes and remotes:FindFirstChild("Data")
    local equipRemote = dataRemotes and dataRemotes:FindFirstChild("EquipCosmetic")
    
    local oldNamecall
    oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
        if getnamecallmethod() ~= "FireServer" then return oldNamecall(self, ...) end
        local args = {...}
        
        if self == equipRemote then
            local weaponName, cosmeticType, cosmeticName, options = args[1], args[2], args[3], args[4] or {}
            local tLower = cosmeticType and cosmeticType:lower() or ""
            local isDanceRemote = (tLower == "dance" or tLower == "emote" or tLower == "taunt" or (cosmeticName and (cosmeticName:lower():find("dance") or cosmeticName:lower():find("emote"))))
            
            if isDanceRemote then
                equipped.Dances = equipped.Dances or {}
                if not cosmeticName or cosmeticName == "None" or cosmeticName == "" then equipped.Dances[cosmeticType] = nil
                else
                    local cloned = cloneCosmetic(cosmeticName, cosmeticType, {inverted = options.IsInverted, favoritesOnly = options.OnlyUseFavorites})
                    if cloned then equipped.Dances[cosmeticType] = cloned end
                end
                task.defer(function() pcall(function() DataController.CurrentData:Replicate("CosmeticInventory") end) task.wait(0.2) saveConfig() end)
                return
            else
                equipped[weaponName] = equipped[weaponName] or {}
                if not cosmeticName or cosmeticName == "None" or cosmeticName == "" then
                    equipped[weaponName][cosmeticType] = nil
                    if not next(equipped[weaponName]) then equipped[weaponName] = nil end
                else
                    local cloned = cloneCosmetic(cosmeticName, cosmeticType, {inverted = options.IsInverted, favoritesOnly = options.OnlyUseFavorites})
                    if cloned then equipped[weaponName][cosmeticType] = cloned end
                end
                task.defer(function() pcall(function() DataController.CurrentData:Replicate("WeaponInventory") end) task.wait(0.2) saveConfig() end)
                return
            end
        end
        return oldNamecall(self, ...)
    end)
end

loadConfig()

-- ==================== ADVANCED UI CREATION SYSTEM ====================

local UI_Accent = Color3.fromRGB(0, 168, 255)       
local UI_Background = Color3.fromRGB(16, 16, 20)    
local UI_Sidebar = Color3.fromRGB(22, 22, 28)       
local UI_Card = Color3.fromRGB(26, 26, 32)          

local HubGui = Instance.new("ScreenGui")
HubGui.Name = "PremiumScriptHub"
HubGui.ResetOnSpawn = false
HubGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

local success, coreTarget = pcall(function() return CoreGui end)
HubGui.Parent = success and coreTarget or player:WaitForChild("PlayerGui")

local MainWindow = Instance.new("Frame")
MainWindow.Size = UDim2.new(0, 560, 0, 360)
MainWindow.Position = UDim2.new(0.5, -280, 0.5, -180)
MainWindow.BackgroundColor3 = UI_Background
MainWindow.BorderSizePixel = 0
MainWindow.Active = true
MainWindow.Parent = HubGui

local MainCorner = Instance.new("UICorner") MainCorner.CornerRadius = UDim.new(0, 10) MainCorner.Parent = MainWindow
local MainStroke = Instance.new("UIStroke") MainStroke.Color = Color3.fromRGB(38, 38, 45) MainStroke.Parent = MainWindow

-- Custom Drag Drop Handling
local dragging, dragInput, dragStart, startPos
MainWindow.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true dragStart = input.Position startPos = MainWindow.Position
        input.Changed:Connect(function() if input.UserInputState == Enum.UserInputState.End then dragging = false end end)
    end
end)
MainWindow.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end
end)
UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        local delta = input.Position - dragStart
        MainWindow.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

-- Sidebar
local Sidebar = Instance.new("Frame")
Sidebar.Size = UDim2.new(0, 150, 1, 0)
Sidebar.BackgroundColor3 = UI_Sidebar
Sidebar.BorderSizePixel = 0
Sidebar.Parent = MainWindow
local SidebarCorner = Instance.new("UICorner") SidebarCorner.CornerRadius = UDim.new(0, 10) SidebarCorner.Parent = Sidebar
local SidebarBlock = Instance.new("Frame") SidebarBlock.Size = UDim2.new(0, 10, 1, 0) SidebarBlock.Position = UDim2.new(1, -10, 0, 0) SidebarBlock.BackgroundColor3 = UI_Sidebar SidebarBlock.BorderSizePixel = 0 SidebarBlock.Parent = Sidebar

local LogoLabel = Instance.new("TextLabel")
LogoLabel.Size = UDim2.new(1, 0, 0, 45)
LogoLabel.Position = UDim2.new(0, 0, 0, 5)
LogoLabel.Text = "RIVALS HUB"
LogoLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
LogoLabel.Font = Enum.Font.GothamBold
LogoLabel.TextSize = 16
LogoLabel.BackgroundTransparency = 1
LogoLabel.Parent = Sidebar

local NavigationList = Instance.new("Frame")
NavigationList.Size = UDim2.new(1, -16, 1, -60)
NavigationList.Position = UDim2.new(0, 8, 0, 50)
NavigationList.BackgroundTransparency = 1
NavigationList.Parent = Sidebar
local NavLayout = Instance.new("UIListLayout") NavLayout.Padding = UDim.new(0, 5) NavLayout.Parent = NavigationList

-- Display Area
local DisplayArea = Instance.new("Frame")
DisplayArea.Size = UDim2.new(1, -160, 1, -15)
DisplayArea.Position = UDim2.new(0, 155, 0, 10)
DisplayArea.BackgroundTransparency = 1
DisplayArea.Parent = MainWindow

local tabsList, framesList, nativeButtons = {"Combat", "Visuals", "Misc", "Skins", "Settings"}, {}, {}

for i, tabName in ipairs(tabsList) do
    local TabBtn = Instance.new("TextButton")
    TabBtn.Size = UDim2.new(1, 0, 0, 32)
    TabBtn.BackgroundColor3 = (tabName == "Skins") and Color3.fromRGB(32, 32, 40) or Color3.fromRGB(0,0,0)
    TabBtn.BackgroundTransparency = (tabName == "Skins") and 0 or 1
    TabBtn.Text = "   " .. tabName
    TabBtn.TextColor3 = (tabName == "Skins") and UI_Accent or Color3.fromRGB(160, 160, 165)
    TabBtn.Font = Enum.Font.GothamMedium
    TabBtn.TextSize = 12
    TabBtn.TextXAlignment = Enum.TextXAlignment.Left
    TabBtn.LayoutOrder = i
    TabBtn.Parent = NavigationList
    local TBtnCorner = Instance.new("UICorner") TBtnCorner.CornerRadius = UDim.new(0, 5) TBtnCorner.Parent = TabBtn
    nativeButtons[tabName] = TabBtn
    
    local TabFrame = Instance.new("ScrollingFrame")
    TabFrame.Size = UDim2.new(1, 0, 1, 0)
    TabFrame.BackgroundTransparency = 1
    TabFrame.BorderSizePixel = 0
    TabFrame.Visible = (tabName == "Skins")
    TabFrame.ScrollBarThickness = 2
    TabFrame.ScrollBarImageColor3 = UI_Accent
    TabFrame.Parent = DisplayArea
    local FrameLayout = Instance.new("UIListLayout") FrameLayout.Padding = UDim.new(0, 8) FrameLayout.Parent = TabFrame
    framesList[tabName] = TabFrame
    
    TabBtn.MouseButton1Click:Connect(function()
        for tName, tBtn in pairs(nativeButtons) do
            TweenService:Create(tBtn, TweenInfo.new(0.2), { BackgroundTransparency = 1, TextColor3 = Color3.fromRGB(160, 160, 165) }):Play()
            framesList[tName].Visible = false
        end
        TweenService:Create(TabBtn, TweenInfo.new(0.2), { BackgroundTransparency = 0, BackgroundColor3 = Color3.fromRGB(32, 32, 40), TextColor3 = UI_Accent }):Play()
        TabFrame.Visible = true
    end)
end

-- COMPONENT GENERATORS
local function CreateSectionCard(parentFrame, titleText)
    local Card = Instance.new("Frame")
    Card.Size = UDim2.new(1, -8, 0, 48)
    Card.BackgroundColor3 = UI_Card
    Card.BorderSizePixel = 0
    Card.Parent = parentFrame
    local CardCorner = Instance.new("UICorner") CardCorner.CornerRadius = UDim.new(0, 6) CardCorner.Parent = Card
    local Title = Instance.new("TextLabel") Title.Size = UDim2.new(0.6, 0, 1, 0) Title.Position = UDim2.new(0, 12, 0, 0) Title.Text = titleText Title.TextColor3 = Color3.fromRGB(225, 225, 230) Title.Font = Enum.Font.GothamMedium Title.TextSize = 12 Title.TextXAlignment = Enum.TextXAlignment.Left Title.BackgroundTransparency = 1 Title.Parent = Card
    return Card
end

local function AttachToggle(cardInstance, defaultState, callback)
    local ToggleBtn = Instance.new("TextButton") ToggleBtn.Size = UDim2.new(0, 45, 0, 22) ToggleBtn.Position = UDim2.new(1, -57, 0.5, -11) ToggleBtn.Text = "" ToggleBtn.Parent = cardInstance
    local ToggleCorner = Instance.new("UICorner") ToggleCorner.CornerRadius = UDim.new(1, 0) ToggleCorner.Parent = ToggleBtn
    local Indicator = Instance.new("Frame") Indicator.Size = UDim2.new(0, 16, 0, 16) Indicator.BackgroundColor3 = Color3.fromRGB(255, 255, 255) Indicator.Parent = ToggleBtn
    local IndicatorCorner = Instance.new("UICorner") IndicatorCorner.CornerRadius = UDim.new(1, 0) IndicatorCorner.Parent = Indicator
    
    local function renderState(state)
        if state then ToggleBtn.BackgroundColor3 = UI_Accent TweenService:Create(Indicator, TweenInfo.new(0.15), {Position = UDim2.new(1, -19, 0.5, -8)}):Play()
        else ToggleBtn.BackgroundColor3 = Color3.fromRGB(50, 50, 60) TweenService:Create(Indicator, TweenInfo.new(0.15), {Position = UDim2.new(0, 3, 0.5, -8)}):Play() end
    end
    renderState(defaultState)
    ToggleBtn.MouseButton1Click:Connect(function() defaultState = not defaultState renderState(defaultState) callback(defaultState) end)
end

local function AttachButton(cardInstance, buttonText, callback)
    local Btn = Instance.new("TextButton") Btn.Size = UDim2.new(0, 80, 0, 24) Btn.Position = UDim2.new(1, -92, 0.5, -12) Btn.BackgroundColor3 = Color3.fromRGB(38, 45, 60) Btn.Text = buttonText Btn.TextColor3 = Color3.fromRGB(255, 255, 255) Btn.Font = Enum.Font.GothamBold Btn.TextSize = 10 Btn.Parent = cardInstance
    local BtnCorner = Instance.new("UICorner") BtnCorner.CornerRadius = UDim.new(0, 4) BtnCorner.Parent = Btn
    Btn.MouseButton1Click:Connect(callback)
end

-- ==================== ADVANCED SKYBOX & SUN/MOON CONTROLLER ====================
local function setSkybox(id, sunId, moonId, clockTime)
    local sky = Lighting:FindFirstChildOfClass("Sky")
    if not sky then
        sky = Instance.new("Sky")
        sky.Name = "CustomHubSky"
        sky.Parent = Lighting
    end
    
    local textureId = "rbxassetid://" .. id
    sky.SkyboxBk = textureId sky.SkyboxDn = textureId sky.SkyboxFt = textureId
    sky.SkyboxLf = textureId sky.SkyboxRt = textureId sky.SkyboxUp = textureId
    
    -- Override Sun and Moon if provided
    if sunId then sky.SunTextureId = "rbxassetid://" .. sunId end
    if moonId then sky.MoonTextureId = "rbxassetid://" .. moonId end
    
    -- Force Time of Day to make Sun/Moon visible
    if clockTime then Lighting.ClockTime = clockTime end
end

-- ==================== POPULATING TAB PANELS ====================

CreateSectionCard(framesList["Combat"], "Aimbot [Placeholder Token]")
CreateSectionCard(framesList["Misc"], "Movement Speed Multiplier [Placeholder]")

-- TIME OF DAY CONTROLS (So you can see the Sun and Moon)
local timeDayCard = CreateSectionCard(framesList["Visuals"], "🌤️ Force Time: DAY (Show Sun)")
AttachButton(timeDayCard, "SET DAY", function() Lighting.ClockTime = 14 end)

local timeNightCard = CreateSectionCard(framesList["Visuals"], "🌙 Force Time: NIGHT (Show Moon)")
AttachButton(timeNightCard, "SET NIGHT", function() Lighting.ClockTime = 0 end)

-- SKYBOXES WITH SUN/MOON SUPPORT
local sky1 = CreateSectionCard(framesList["Visuals"], "🔴 Blood Moon Night (Custom Moon)")
AttachButton(sky1, "APPLY", function() setSkybox("217800918", nil, "813965516", 0) end) -- Night sky, scary red moon

local sky2 = CreateSectionCard(framesList["Visuals"], "⚫ Black Hole Sun (Custom Sun)")
AttachButton(sky2, "APPLY", function() setSkybox("159454286", "233880816", nil, 14) end) -- Galaxy sky, black hole sun

local sky3 = CreateSectionCard(framesList["Visuals"], "🌇 Vaporwave City")
AttachButton(sky3, "APPLY", function() setSkybox("1061734346") end)

local sky4 = CreateSectionCard(framesList["Visuals"], "⚔️ Anime Sky")
AttachButton(sky4, "APPLY", function() setSkybox("1045964490") end)

local sky5 = CreateSectionCard(framesList["Visuals"], "🌌 Deep Purple Nebula")
AttachButton(sky5, "APPLY", function() setSkybox("159454299", nil, nil, 0) end)

local skyReset = CreateSectionCard(framesList["Visuals"], "❌ Reset Lighting")
AttachButton(skyReset, "RESET", function()
    local sky = Lighting:FindFirstChild("CustomHubSky")
    if sky then sky:Destroy() end
end)

-- SKINS TAB
local card1 = CreateSectionCard(framesList["Skins"], "Skin Changer") AttachToggle(card1, Toggles.SkinChanger, function(val) Toggles.SkinChanger = val end)
local card2 = CreateSectionCard(framesList["Skins"], "Charm Changer") AttachToggle(card2, Toggles.CharmChanger, function(val) Toggles.CharmChanger = val end)
local card3 = CreateSectionCard(framesList["Skins"], "Wrap Changer") AttachToggle(card3, Toggles.WrapChanger, function(val) Toggles.WrapChanger = val end)
local card4 = CreateSectionCard(framesList["Skins"], "Dance & Emote Changer") AttachToggle(card4, Toggles.DanceChanger, function(val) Toggles.DanceChanger = val end)

-- SETTINGS TAB
local configCard = CreateSectionCard(framesList["Settings"], "Save Settings State Profile")
AttachButton(configCard, "SAVE", function()
    saveConfig()
    local btn = configCard:FindFirstChildOfClass("TextButton")
    if btn then btn.Text = "SAVED!" task.wait(1) btn.Text = "SAVE" end
end)

local unloadCard = CreateSectionCard(framesList["Settings"], "Unload & Close Menu GUI")
AttachButton(unloadCard, "UNLOAD", function() HubGui:Destroy() end)
unloadCard:FindFirstChildOfClass("TextButton").BackgroundColor3 = Color3.fromRGB(90, 35, 35)

UserInputService.InputBegan:Connect(function(input, processed)
    if not processed and input.KeyCode == Toggles.ToggleKey then MainWindow.Visible = not MainWindow.Visible end
end)

print("Premium Custom Layout Dashboard Loaded Successfully.")
